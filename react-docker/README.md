# Docker Course – Hands-on Learning Project

This repository documents my practical learning journey with **Docker**, focusing on containerization, image building, and basic CI/CD workflows.  
The goal of this project is to demonstrate real, working knowledge of Docker concepts through hands-on exercises rather than theory.

---

## 🔧 Technologies Used
- Docker
- Docker Hub
- GitHub Actions (CI)
- Linux (CLI)
- YAML

---

## 📌 What This Project Covers (So Far)

- Writing Dockerfiles
- Building Docker images locally
- Tagging and pushing images to Docker Hub
- Using Docker CLI commands (`build`, `run`, `push`, `pull`)
- Authenticating to Docker Hub using Personal Access Tokens
- Basic CI pipeline with GitHub Actions for Docker builds

---

## 🚀 Current Progress

- ✅ Docker installed and configured
- ✅ Dockerfile created and tested
- ✅ Image successfully built locally
- ✅ Docker Hub authentication implemented
- ✅ Image pushed to Docker Hub
- 🔄 CI pipeline improvements in progress/
